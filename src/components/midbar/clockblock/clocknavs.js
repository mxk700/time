import React, {Component} from 'react';

class ClockNavs extends Component {
  render(){
      return (
        <div id="clockNavs">
          <button>insert   </button>
          <button>start</button>
        </div>
      )
  }
};


export default ClockNavs;
